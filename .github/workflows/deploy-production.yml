name: Deploy Production

# Self-hosted CD for Multica's Mac mini production host.
#
# Triggers:
#   - push to main: continuous deploy on every merge.
#   - workflow_dispatch: manual / Ship-Hub-driven re-deploy. Useful for
#     re-running after a transient docker build failure, or for shipping
#     a release in flight when "Mark production deployed" was skipped.
#
# Trust model:
#   - Runs ONLY on push to main + manual dispatch. No pull_request
#     trigger — that would let any fork PR execute arbitrary code on the
#     Mac mini. Branch protection on main is the user's safety floor:
#     only merged PRs land here.
#   - Runs on a self-hosted runner labeled `mac-mini-prod`. The runner
#     binary itself is the one piece of trust on the host; everything
#     else flows from "what's currently on main".
#
# Wired into Ship Hub:
#   - Workspace setting `ship_hub_deploy_workflow_production` is set to
#     `deploy-production.yml` so the deploy-workflow poller watches this
#     workflow's runs. On `conclusion=success` with a matching SHA, the
#     poller flips the release stage from `promoting` to `in_production`
#     within 2 minutes. The deployment-status step below is an optional
#     accelerator (~immediate) for the same transition.

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      sha:
        description: "Specific commit SHA to deploy (defaults to main HEAD)"
        required: false
        type: string

# Only one production deploy at a time. New pushes queue, then run with
# `cancel-in-progress: false` so we don't mid-build kill a deploy that's
# already touching the container state. Better to deploy stale-then-
# fresh than to leave the host in a partially-deployed state.
concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  deploy:
    name: Build + restart containers
    runs-on: [self-hosted, macOS, mac-mini-prod]
    timeout-minutes: 30
    permissions:
      contents: read
      deployments: write
    env:
      # Docker Desktop's credential helper lives in this path; the
      # default shell PATH on a non-login ssh / runner shell omits it
      # and `docker compose build` then fails with
      # "docker-credential-desktop: executable not found".
      DOCKER_PATH_EXTRAS: /usr/local/bin:/Applications/Docker.app/Contents/Resources/bin
      # Note: project name (`multica-prod`) is set authoritatively in
      # docker-compose.selfhost.yml via its top-level `name:` field, so
      # there's no need to set COMPOSE_PROJECT_NAME here. (The legacy
      # `COMPOSE_PROJECT: multica` env line was unused and is gone.)
      COMPOSE_FILES: -f docker-compose.selfhost.yml -f docker-compose.selfhost.build.yml
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # `workflow_dispatch` lets the caller pin a specific SHA;
          # otherwise we take whatever main is at right now.
          ref: ${{ github.event.inputs.sha || github.ref }}
          # Full history isn't needed for the build; shallow checkout
          # speeds up the runner. If a future step needs `git log`,
          # bump this.
          fetch-depth: 1

      - name: Capture build SHA for /build_info injection
        # The Go binary embeds main.commit via -ldflags so the
        # /build_info endpoint can report what's actually running.
        # We compute the SHA from the checked-out workdir (so a
        # workflow_dispatch with a custom input.sha gets the SHA
        # that was actually checked out, not whatever main is at
        # right now). docker-compose.selfhost.build.yml reads
        # MULTICA_BUILD_COMMIT as a build arg.
        run: |
          SHA="$(git rev-parse HEAD)"
          echo "MULTICA_BUILD_COMMIT=${SHA}" >> "$GITHUB_ENV"
          echo "Build SHA: ${SHA}"

      - name: Materialize operator .env from stable host path
        # actions/checkout wipes untracked files in the workdir on
        # every run, including any .env the operator dropped there
        # manually. Operators store their per-host config (port
        # overrides, secrets, etc.) at ~/multica-deploy.env (a path
        # outside the runner workdir, so it survives checkout); we
        # copy it in here so docker compose AND the preflight step
        # both see the same values.
        run: |
          if [ -f "${HOME}/multica-deploy.env" ]; then
            cp "${HOME}/multica-deploy.env" .env
            echo "Loaded operator config from ~/multica-deploy.env"
          else
            echo "No ~/multica-deploy.env on this host; using compose defaults."
          fi

      # Record the deployment in GitHub so the timeline + Ship Hub get
      # an immediate signal instead of waiting for the 2-min poller.
      - name: Create deployment
        id: deployment
        uses: actions/github-script@v7
        with:
          script: |
            const { data } = await github.rest.repos.createDeployment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: context.sha,
              environment: 'production',
              description: 'Mac mini self-hosted deploy',
              auto_merge: false,
              required_contexts: [],
              production_environment: true,
            });
            // Some API shapes return an error object on conflict; guard.
            if (!data?.id) {
              core.setFailed(`createDeployment did not return an id: ${JSON.stringify(data)}`);
              return;
            }
            core.setOutput('deployment_id', data.id);

      - name: Mark deployment in_progress
        if: steps.deployment.outputs.deployment_id
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.repos.createDeploymentStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              deployment_id: ${{ steps.deployment.outputs.deployment_id }},
              state: 'in_progress',
              description: 'Building images and recreating containers',
              environment: 'production',
              auto_inactive: false,
            });

      - name: Stop orphaned containers from prior builds
        # ONLY sweeps containers from OTHER project namespaces — never
        # touches our own `multica-prod-*` containers. Pre-this-change,
        # this step did `docker compose down` on our own project at the
        # start of every run, which meant a workflow cancelled after
        # `down` but before `up` left the stack DARK. That's exactly
        # what happened on 2026-05-12 when a 6-PR merge-train fired 7
        # deploys in 14s, several got cancelled mid-cycle, and prod
        # was unreachable for ~2.5 minutes.
        #
        # Post-this-change, the down step is gone. We rely on
        # `docker compose up -d --build` to handle recreation
        # idempotently:
        #   - rebuilds images whose source changed
        #   - recreates containers whose image hash moved
        #   - leaves running containers alone otherwise
        # A cancellation between this step and `up` is now a no-op
        # rather than an outage.
        #
        # The sweep still handles two real cases:
        #
        # 1. Legacy `multica-*` containers from the pre-rename era
        #    (pre-PR #46, when selfhost used `name: multica`).
        #    One-time scrub on the first post-rename deploy.
        # 2. Stray `multica-*` containers from agent task workspaces
        #    that share the docker engine. Each workspace's
        #    `docker-compose.yml` declares `name: multica` (the
        #    dev/worktree default), so any `make dev` / `make test`
        #    from an agent task creates conflicting container names.
        #
        # The filter explicitly excludes `multica-prod-*` so we never
        # take out our own production containers, running or not.
        run: |
          export PATH="${DOCKER_PATH_EXTRAS}:${PATH}"
          set -e
          for n in $(docker ps -a --format '{{.Names}}' | grep '^multica-' | grep -v '^multica-prod-' || true); do
            echo "Removing orphan (non-prod) container $n"
            docker rm -f "$n" || true
          done

      - name: Report current stack state
        # Pre-deploy visibility: log what's currently running under our
        # project name. Used by operators triaging cascade incidents
        # ("was prod up when this deploy started?") and by the
        # build-SHA verification follow-on (compares to the SHA the
        # post-deploy step proves).
        run: |
          export PATH="${DOCKER_PATH_EXTRAS}:${PATH}"
          running=$(docker ps --filter "label=com.docker.compose.project=multica-prod" --format '{{.Names}} {{.Status}}')
          if [ -z "$running" ]; then
            echo "::warning::No multica-prod containers running on this host before deploy."
            echo "If this deploy is recovering from a cancelled run, that's expected."
          else
            echo "Current multica-prod containers:"
            echo "$running"
          fi

      - name: Pre-flight — required host ports are free
        # If something OUTSIDE our own docker project holds the port
        # we're about to bind — another tenant container on this Mac
        # mini (pisignage-server, winery-manager, etc.), a homebrew
        # node, an old `pnpm dev:web`, a PM2-managed service, a
        # launchd job — docker fails with the unhelpful
        # `Bind for 0.0.0.0:PORT failed` *only after* a full image
        # rebuild that can take 5+ minutes. Detect early and fail
        # loud with the offending pid so the operator knows what to
        # kill on the host or which `*_PORT` env var to retarget.
        #
        # We check the host ports that compose will try to bind:
        #   - backend:  ${PORT:-8080}
        #   - frontend: ${FRONTEND_PORT:-3000}
        # (postgres no longer publishes a host port — see PR #26.)
        #
        # **Skip ports held by our OWN multica-prod-* containers.**
        # Pre-PR #53, the workflow always ran `docker compose down`
        # before this check, so any port we wanted was free by
        # definition. PR #53 removed that down to survive cancelled
        # deploys — but that left this preflight failing on the
        # workflow's FIRST run after PR #53 landed (own containers
        # still up, holding the ports). The fix: ignore ports held
        # by our own project's containers and only fail on truly
        # foreign holders.
        #
        # Source the operator's .env first so the values here line up
        # with what docker compose will actually use — otherwise the
        # preflight checks the defaults (3000/8080) even when .env
        # retargets the frontend onto a different host port.
        run: |
          export PATH="${DOCKER_PATH_EXTRAS}:${PATH}"
          if [ -f .env ]; then
            set -o allexport
            # shellcheck disable=SC1091
            . ./.env
            set +o allexport
          fi
          BACKEND_PORT="${PORT:-8080}"
          FRONTEND_HOST_PORT="${FRONTEND_PORT:-3000}"
          # Build the set of ports OUR project is already publishing,
          # so we don't fail the preflight on our own containers.
          our_ports=$(docker ps --filter "label=com.docker.compose.project=multica-prod" --format '{{.Ports}}' \
                       | grep -oE '0\.0\.0\.0:[0-9]+' | cut -d: -f2 | sort -u)
          fail=0
          for port in "$BACKEND_PORT" "$FRONTEND_HOST_PORT"; do
            holder=$(lsof -nP -iTCP:"$port" -sTCP:LISTEN 2>/dev/null | tail -n +2 || true)
            if [ -z "$holder" ]; then
              echo "Port $port is free."
              continue
            fi
            # Is the holder one of our own multica-prod-* containers?
            if echo "$our_ports" | grep -qx "$port"; then
              echo "Port $port is held by an existing multica-prod container — OK (compose up will recreate it in place)."
              continue
            fi
            echo "::error::Port $port is bound by something outside our project:"
            echo "$holder"
            fail=1
          done
          if [ $fail -eq 1 ]; then
            echo ""
            echo "Free the port(s) above (kill the process or stop the service)"
            echo "and re-run the workflow. The orphan-container sweep above"
            echo "only handles multica's own docker-managed containers — anything"
            echo "outside our project shows up here and needs operator action."
            exit 1
          fi

      - name: Build + restart containers
        # `up -d --build` is idempotent: rebuilds images that changed,
        # recreates only containers whose image hash moved, leaves
        # postgres alone if its image hash is unchanged. Single command
        # rather than separate build + restart because compose handles
        # the "did anything change" question better than we would
        # scripting it.
        #
        # We explicitly include postgres in the service list so a
        # missing-postgres state (e.g. after a cascade incident or a
        # daemon restart) gets reconciled — `up postgres frontend
        # backend` is no-op if postgres is already running, and brings
        # it back if it isn't. Pre-this-change the command only named
        # frontend + backend, which left a missing-postgres situation
        # silently stuck.
        run: |
          export PATH="${DOCKER_PATH_EXTRAS}:${PATH}"
          docker compose ${COMPOSE_FILES} up -d --build postgres frontend backend

      - name: Health check
        # Curl the published frontend port from the runner. The host
        # port comes from FRONTEND_PORT in the operator .env (default
        # 3000); we re-source it here for the same reason as preflight.
        #
        # Previous attempts tried docker compose exec + wget inside
        # the container; the Next.js standalone alpine image doesn't
        # ship wget, so the command silently returned an empty status
        # and the deploy waited the full 60s for nothing. Hitting the
        # exposed port from the host avoids the in-container tooling
        # problem entirely.
        #
        # We poll for up to ~60s because Next.js can take 5-15s to
        # actually accept traffic after the container reports started.
        run: |
          if [ -f .env ]; then
            set -o allexport
            # shellcheck disable=SC1091
            . ./.env
            set +o allexport
          fi
          FRONTEND_HOST_PORT="${FRONTEND_PORT:-3000}"
          for i in $(seq 1 30); do
            code=$(curl -s -o /dev/null -w "%{http_code}" \
                     "http://localhost:${FRONTEND_HOST_PORT}/" || echo 000)
            if [ "$code" = "200" ]; then
              echo "Frontend healthy after ${i} attempts (code=${code}) on port ${FRONTEND_HOST_PORT}"
              exit 0
            fi
            echo "Attempt ${i}: code=${code} on port ${FRONTEND_HOST_PORT}, retrying in 2s..."
            sleep 2
          done
          echo "Frontend never returned 200 within 60s; failing deploy."
          # Dump recent backend + frontend logs so the failed run gives
          # us something to grep when triaging.
          export PATH="${DOCKER_PATH_EXTRAS}:${PATH}"
          docker compose ${COMPOSE_FILES} logs --tail 50 backend frontend || true
          exit 1

      - name: Verify build SHA matches the running container
        # Post-health-check assertion that the backend container is
        # actually serving the SHA we just built — not a stale image
        # from a half-finished prior deploy. Closes the 2026-05-12
        # 17:06 UTC failure mode where env.current_sha showed e72c66dd
        # but the host was actually running ea58198.
        #
        # We curl the backend's /build_info endpoint (unauthenticated,
        # returns {commit, version, go_version}) from the runner's
        # localhost. If the commit doesn't match MULTICA_BUILD_COMMIT,
        # the deploy fails — the runtime answer wins, the workflow's
        # belief about what was deployed does not.
        run: |
          if [ -f .env ]; then
            set -o allexport
            # shellcheck disable=SC1091
            . ./.env
            set +o allexport
          fi
          BACKEND_HOST_PORT="${PORT:-8080}"
          # Poll briefly — backend might be up but /build_info is at the
          # tail end of its initialization. 10x 1s is more than enough.
          for i in $(seq 1 10); do
            running_commit=$(curl -s "http://localhost:${BACKEND_HOST_PORT}/build_info" \
                              | grep -o '"commit":"[^"]*"' \
                              | cut -d'"' -f4 || true)
            if [ -n "$running_commit" ]; then break; fi
            sleep 1
          done
          if [ -z "$running_commit" ]; then
            echo "::error::Could not read /build_info from running backend after 10s"
            exit 1
          fi
          if [ "$running_commit" = "unknown" ]; then
            echo "::error::Backend reports commit='unknown' — image was built without -ldflags injection. Refusing to mark deploy successful."
            exit 1
          fi
          if [ "$running_commit" != "$MULTICA_BUILD_COMMIT" ]; then
            echo "::error::Build SHA mismatch! Workflow deployed ${MULTICA_BUILD_COMMIT} but container serves ${running_commit}"
            echo "This is the 2026-05-12 stale-container failure mode. Refusing to mark deploy successful."
            exit 1
          fi
          echo "Verified: running container serves ${running_commit} (matches deploy target)"

      - name: Mark deployment success
        if: success() && steps.deployment.outputs.deployment_id
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.repos.createDeploymentStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              deployment_id: ${{ steps.deployment.outputs.deployment_id }},
              state: 'success',
              description: 'Containers healthy on multica.wisco.wine',
              environment: 'production',
              environment_url: 'https://multica.wisco.wine',
              log_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
              auto_inactive: true,
            });

      - name: Mark deployment failure
        if: failure() && steps.deployment.outputs.deployment_id
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.repos.createDeploymentStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              deployment_id: ${{ steps.deployment.outputs.deployment_id }},
              state: 'failure',
              description: 'Deploy failed — check workflow logs',
              environment: 'production',
              log_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
            });
